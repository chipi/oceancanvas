{
  "name": "oceancanvas-renderer",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Headless p5.js renderer — JSON stdin, PNG stdout",
  "scripts": {
    "test": "node --test test/"
  },
  "dependencies": {
    "puppeteer": "^22.0.0"
  }
}
