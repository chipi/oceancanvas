import { describe, it } from 'node:test';
import assert from 'node:assert/strict';

describe('renderer', () => {
  it('placeholder — render.js exists', () => {
    // Real tests will spawn render.js with fixture payloads
    // and verify PNG output. Placeholder for scaffold.
    assert.ok(true, 'renderer test scaffold ready');
  });
});
